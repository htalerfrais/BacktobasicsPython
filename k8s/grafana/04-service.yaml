apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: backtobasics
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: backtobasics
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30300
