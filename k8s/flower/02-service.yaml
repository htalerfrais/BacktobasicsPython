apiVersion: v1
kind: Service
metadata:
  name: flower
  namespace: backtobasics
  labels:
    app.kubernetes.io/name: flower
    app.kubernetes.io/part-of: backtobasics
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: flower
  ports:
    - name: http
      port: 5555
      targetPort: http
      nodePort: 30555
